VITE+ Intent workflow for this repository

- List npm-provided skills: `vp exec intent list` (or `pnpm intent:list`)
- Install/update skill mappings: `vp exec intent install` (or `pnpm intent:install`)
- Default mappings target: `AGENTS.md` with an `intent-skills` block
- Re-run install whenever dependencies that provide skills are added or updated
