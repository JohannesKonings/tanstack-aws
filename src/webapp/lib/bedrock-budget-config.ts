/**
 * Backend config for Bedrock budget. Single source of truth for the daily limit.
 * Used by bedrock-budget.ts and API routes (api.bedrock-budget, api.tanchat).
 */

/** Daily budget limit in USD ($2.00). */
export const DAILY_LIMIT_USD = 2
